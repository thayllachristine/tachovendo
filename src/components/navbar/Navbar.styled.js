import styled from 'styled-components';

const Navbar = styled.nav`
  padding: 16px;
`;

const Link = styled.a`
  color: #ffffff;
  cursor: pointer;
`;

const Icon = styled.i`
  font-size: 36px;
`;

export { Navbar, Link, Icon };
