import styled from 'styled-components';

const Navbar = styled.nav`
  padding: 24px 16px;
  position: absolute;
`;

const Link = styled.a`
  color: #ffffff;
  cursor: pointer;
`;

const Icon = styled.i`
  font-size: 36px;
`;

export { Navbar, Link, Icon };
